# 패키지 위험 정책

초기 정책은 “넓은 승인, 강한 차단”이다. 골든 템플릿과 주요 Track에서 널리 쓰이는 패키지는 기본 승인하되, 행정망·DMZ 운영에 맞지 않는 패턴은 강하게 막는다.

## 등급

| 등급 | 의미 | 처리 |
|---|---|---|
| core | 골든 템플릿 기본 패키지 | 자유 사용 |
| approved | 검토된 일반 패키지 | 사용 가능 |
| restricted | 조건부 사용 | 경고와 조건 확인 |
| exception-required | 기본 미승인 | 예외신청 필요 |
| denied | 금지 | 대체안 안내, 운영 반입 차단 |

## 강차단 기준

- Critical CVE 또는 악성 패키지 신고
- 외부 BaaS 직접 의존(supabase-js, firebase 등)
- CDN 운영 의존
- npm postinstall 필수
- eval/new Function 등 임의 코드 실행
- 금지 라이선스
- 유지보수 중단
- 비밀정보 수집 또는 외부전송 기능

## 경고·검토 기준

- 기존 패키지의 typosquat 의심 신호

타이포스쿼팅 탐지는 휴리스틱이므로 하네스에서는 단독 차단 근거로 쓰지 않는다. 사용자에게 경고하고 패키지명 확인, 대체 패키지, 담당자 검토를 제안한다. 다만 공식 저장소에 없는 `not_found`는 휴리스틱이 아니라 사실 확인이므로 강차단한다. 레지스트리는 typosquat 신호가 있는 패키지를 자동 승인하지 않고 `UNDER_REVIEW`로 보낼 수 있다.

## 운영

unknown 패키지는 바로 차단하지 않고 승인 패키지 대체안을 먼저 제시한다. 대체가 어려우면 패키지 예외신청서를 생성한다.

## 거버넌스 연결

상태값과 승격 규칙의 단일 기준은 `shared/references/package-governance.yaml`이다. 이 문서는 위험 패턴의 설명서이며, 최종 승인/불허 원장은 아니다.

플랫폼이 개발되기 전에는 하네스가 다음 순서로 판단한다.

1. 골든 템플릿 기본 패키지 여부
2. 기관 프로파일의 추가 허용/제한/차단
3. denylist와 위험 패턴
4. `vibecode-checker scan_dependencies` verdict와 증거
5. 담당자 검토 또는 예외신청

플랫폼이 개발되면 플랫폼과 레지스트리가 승인/불허 원장이 되고, 하네스는 직접 레지스트리를 호출하지 않고 `vibecode-checker/gvskb`가 돌려준 registry-backed verdict를 집행한다.
