{
  "name": "vibecode-security-gate-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Local web portal for public-sector vibe coding security checks.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/scan-summary.mjs && node --check scripts/portal-harness-gate.mjs && node --check scripts/portal-button-contract-test.mjs && node scripts/portal-folder-picker-test.mjs && node scripts/scan-summary-test.mjs",
    "harness:gate": "node scripts/portal-harness-gate.mjs .",
    "security:scan": "gvskb scan src --profile dev-quick --format json --output reports/dev-quick-src --max-files 200 --fail-on block",
    "button:test": "node scripts/portal-button-contract-test.mjs",
    "db:check": "node scripts/db-schema-structure-check.mjs",
    "scenario:test": "node scripts/portal-scenario-test.mjs",
    "hwpx:test": "node scripts/hwpx-template-test.mjs",
    "hwpx:validate": "node scripts/hwpx-validate.mjs",
    "guard": "npm run check && npm run hwpx:test && npm run harness:gate && npm run button:test && npm run scenario:test && npm run security:scan"
  },
  "dependencies": {
    "busboy": "^1.6.0"
  }
}
